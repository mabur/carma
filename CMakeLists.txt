cmake_minimum_required(VERSION 3.5)
project(carma)
FILE(GLOB source "src/*.c" "src/*.h")
add_executable(carma ${source})
target_link_libraries(carma PRIVATE)
target_compile_features(carma PRIVATE c_std_99)
